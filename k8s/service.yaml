apiVersion: v1
kind: Service
metadata:
  name: weedmaps-demo
  namespace: weedmaps-demo
  labels:
    app: weedmaps-demo
spec:
  selector:
    app: weedmaps-demo
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
