apiVersion: v1
kind: Service
metadata:
  name: wm-demo
  namespace: wm-demo
  labels:
    app: wm-demo
spec:
  selector:
    app: wm-demo
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
